---
description: Iniciar el servidor de desarrollo del frontend
---

Ejecuta el comando de `pnpm run dev`.

Si estás en la raíz del proyecto, primero muévete a la carpeta `frontend/` antes de iniciar el servidor:

```bash
cd frontend
pnpm run dev
```