---
description: Iniciar el servidor de desarrollo del backend
---

Ejecuta el comando de `uvicorn main:app --reload`.

Si estás en la raíz del proyecto, primero muévete a la carpeta `backend/` antes de iniciar el servidor:

```bash
cd backend
uvicorn main:app --reload
```